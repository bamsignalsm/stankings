# Observability Foundation

`buildPlatformHealthReport()` + `correlateEventLog(event)` for structured logs with correlation/causation metadata.

No third-party APM required. Events remain transport-agnostic.
