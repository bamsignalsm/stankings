import { LaunchWarRoomPanel } from "@/components/launch-war-room/LaunchWarRoomPanel";

export const metadata = {
  title: "Founder Dashboard — Ecosystem Execution Board",
  robots: { index: false, follow: false },
};

export default function LaunchWarRoomPage() {
  return <LaunchWarRoomPanel />;
}
